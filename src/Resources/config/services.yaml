services:
    _defaults:
        autowire: true
        autoconfigure: true
        public: false

    # This bundle only contains request DTOs which are not registered as services.
    # Request classes should be instantiated directly when needed.